import { stitches } from '@/lib/config/stitches.config';

const FlexCenter = stitches.styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});

export default FlexCenter;
