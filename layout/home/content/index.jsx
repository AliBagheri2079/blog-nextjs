'use client';
import ImageItems from './image-items';
import TitleSlides from './title-slides';

const Content = () => {
  return (
    <main className='min-h-screen'>
      <TitleSlides />
      <ImageItems />
    </main>
  );
};

export default Content;
